(()=>{'use strict';const buttons=document.querySelectorAll('[data-journal-filter]'),cards=document.querySelectorAll('.blog-card[data-topic]'),status=document.querySelector('#journal-status');buttons.forEach(button=>button.addEventListener('click',()=>{const topic=button.dataset.journalFilter;buttons.forEach(b=>b.setAttribute('aria-pressed',String(b===button)));let count=0;cards.forEach(card=>{const show=topic==='all'||card.dataset.topic===topic;card.hidden=!show;if(show)count++});if(status)status.textContent=`${count} ${count===1?'guide':'guides'} to explore`;}));})();
